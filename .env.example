# Upstream GraphQL endpoint that the SvelteKit proxy forwards requests to.
GRAPHQL_ENDPOINT="https://graph.warerastats.io/query"

# Bearer token injected by the server-side proxy. Keep this secret.
GRAPHQL_TOKEN=""
